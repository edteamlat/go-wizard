package {{ parseToLower .Model }}

type Storage interface {
	GetTx() (model.Transaction, error)

	Create(m *model.{{ .Model }}) error
	Update(m *model.{{ .Model }}) error
	Delete(ID uint) error

	GetWhere(specification Specification) (model.{{ .Model }}, error)
	GetAllWhere(specification Specification) (model.{{ parseToUpperCamelCase .Table }}, error)
}
